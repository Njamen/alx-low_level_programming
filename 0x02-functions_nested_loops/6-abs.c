#include "main.h"
/* more headers goes there */

/**
 * _abs - check is a randomly given number is positive or negativ
 * @n: given char
 *
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	};
}
